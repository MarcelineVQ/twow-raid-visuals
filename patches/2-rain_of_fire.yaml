#foo 8100 base

SpellVisualKit.dbc:
  - type: copy
    key: 692
    values:
      ID: 8100
      WorldEffect: 4801 #add a prehit animation, slightly speeds up players seeing the RoF

SpellVisual.dbc:
  - type: copy
    key: 329
    values:
      ID: 8100
      StateKit: 668
      AreaModel: 4902
      MissileModel: 1179
      # MissilePathType: 2
      # MissileDestinationAttachment: 0
      AreaKit: 8100
      HasMissile: 1 #disable secondary ground effect renderings

Spell.dbc:
  - type: update
    key: 19717 #gehennas and blackwing warlockk(19717)
    values:
      SpellVisualID_1: 8100
      Speed: 255.0
  - type: update
    key: 28794 #faerlina and bovar'kez(28794)
    values:
      SpellVisualID_1: 8100
      Speed: 255.0

# todo for some reason dragging people in raid frame drops frames bad fr me, not sure when that started

# todo make signups

# rupturan sizes were perrrrfect, maybe a tiny bit wider on smaller adds, anomaly needs changing
# rupturan affects faded a little fast
